# Environment defaults
# .env.local (gitignored) can override these locally.

# Upstream proxy target
PROXY_TARGET=https://pokeapi.co/api/v2/
# Server port preference (will auto-shift if busy)
PORT=4000
# Optional: comma-separated allowed CORS origins (leave blank for all)
# CORS_ORIGINS=
