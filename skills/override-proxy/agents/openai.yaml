interface:
  display_name: "override-proxy"
  short_description: "Build override-proxy configs and rules"
  default_prompt: "Use $override-proxy to add a local HTTP override rule and validate the config."
