interface:
  display_name: "override-proxy"
  short_description: "Configure an isolated override-proxy workspace"
  default_prompt: "Use $override-proxy to choose the appropriate workspace, create a local HTTP override rule, and validate the config with that workspace's package manager."
